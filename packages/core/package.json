{
  "name": "@stdyapp/core",
  "version": "0.0.0",
  "description": "The core logic of stdyapp!",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "echo \"no dev step for @stdyapp/core\" && exit 0",
    "build": "echo \"no build step for @stdyapp/core\" && exit 0",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1121.0",
    "@prisma/client": "^6.10.1",
    "amqp-connection-manager": "^5.0.0",
    "amqplib": "^2.0.1",
    "ioredis": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.10.1"
  },
  "license": "Apache-2.0"
}
