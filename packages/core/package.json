{
  "name": "@stdyapp/core",
  "version": "0.0.0",
  "description": "The core logic of stdyapp!",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"Error: no dev step for shared\" && exit 0",
    "build": "echo \"Error: no build step for shared\" && exit 0"
  },
  "dependencies": {}
}
