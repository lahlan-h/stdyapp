{
  "name": "@stdyapp/ui",
  "version": "0.0.0",
  "description": "Shared UI of the stdyapp.",
  "private": "true",
  "type": "module",
  "scripts": {
    "dev": "echo \"Error: no dev step for shared\" && exit 0",
    "build": "echo \"Error: no build step for shared\" && exit 0"
  },
  "dependencies": {}
}
